import Input from "./Input/Input";

const NumberOfAdultsInput = (props) => {
    return (
        <Input fieldName="numberOfAdults" label="Number of Adults" {...props} />
    );
};

export default NumberOfAdultsInput;
